apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: hybrid-todo              
  namespace: todo-app
  labels:
    app: hybrid-todo
spec:
  channel:  simple-app/federation
  placement: 
    placementRef: 
      kind: PlacementRule
      name: prod-only     
